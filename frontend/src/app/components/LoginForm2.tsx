import { redirect } from "next/navigation";
// import { getSession, login, logout } from "../lib";
// import { getSession, login, logout } from "";

export default async function LoginForm2() {
  const session = await getSession();
  return (
    <section>
    </section>
    );
}